import CombaTracker from "@/components/container/CombaTracker";

export default async function Combat() {

  return (
    <main className="relative flex max-screen min-h-screen flex-col items-center justify-between">
     <CombaTracker/>
    </main>
  );
}